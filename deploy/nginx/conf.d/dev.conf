# Development environment configuration
# HTTP configuration - for initial setup and Let's Encrypt verification
server {
    listen 80;
    server_name dev.koalabudget.com;

    # Let's Encrypt verification
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Static files
    location /static/ {
        alias /static/;
        expires 1d;
        add_header Cache-Control "public";
    }

    # Proxy all other requests to Django (development)
    location / {
        resolver 127.0.0.11 valid=30s;
        set $upstream http://koala-web-dev:8001;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
        proxy_buffering off;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# HTTPS configuration - uncomment after obtaining SSL certificate via:
#   ./deploy/init-ssl.sh dev dev.koalabudget.com your@email.com
# server {
#     listen 443 ssl http2;
#     server_name dev.koalabudget.com;
#
#     ssl_certificate /etc/letsencrypt/live/dev.koalabudget.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dev.koalabudget.com/privkey.pem;
#
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # Let's Encrypt verification
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     # Static files
#     location /static/ {
#         alias /static/;
#         expires 1d;
#         add_header Cache-Control "public";
#     }
#
#     # Proxy all other requests to Django (development)
#     location / {
#         proxy_pass http://koala-web-dev:8001;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_redirect off;
#         proxy_buffering off;
#
#         # WebSocket support
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
#
# # HTTP to HTTPS redirect for dev
# server {
#     listen 80;
#     server_name dev.koalabudget.com;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
